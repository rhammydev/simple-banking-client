# Base URL of the Simple Banking API.
# Leave unset in development to use the Vite proxy (see vite.config.ts) -> http://localhost:5018
# Set to the deployed API origin in production, e.g. https://api.prestigebanking.ng
VITE_API_BASE_URL=/api
